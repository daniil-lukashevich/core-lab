@page "/test"
@model RazorPages.Pages.HelpersModel
@{
}

<h1>Helpers</h1>

@using (Html.BeginForm())
{
    <div>
        @Html.LabelFor(m => m.Name)
        @Html.EditorFor(m => m.Name)
    </div>
    <div>
        @Html.LabelFor(m => m.Price)
        @Html.EditorFor(m => m.Price)
    </div>
    <div>
        @Html.DropDownListFor(m => m.CompanyId, new List<SelectListItem>()
        {
            new SelectListItem(){ Text = "My test", Selected = true, Value="test"}
        })
    </div>
    <div>
        <input type="submit" value="Save" />
    </div>
}